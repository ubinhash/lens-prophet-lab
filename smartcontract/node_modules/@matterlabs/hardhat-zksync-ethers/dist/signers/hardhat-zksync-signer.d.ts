import { Provider, Signer } from 'zksync-ethers';
import { TransactionRequest, TransactionResponse } from 'zksync-ethers/build/types';
import { ethers } from 'ethers';
import { HardhatRuntimeEnvironment } from 'hardhat/types';
export declare class HardhatZksyncSigner extends Signer {
    private accountWallet;
    private constructor();
    static create(hre: HardhatRuntimeEnvironment, provider: Provider, address: string): Promise<HardhatZksyncSigner>;
    sendTransaction(transaction: TransactionRequest): Promise<TransactionResponse>;
    signMessage(message: string | Uint8Array): Promise<string>;
    signTypedData(domain: ethers.TypedDataDomain, types: Record<string, ethers.TypedDataField[]>, value: Record<string, any>): Promise<string>;
    signTransaction(transaction: TransactionRequest): Promise<string>;
    private _prepareTransaction;
    protected populateFeeData(transaction: TransactionRequest): Promise<ethers.PreparedTransactionRequest>;
    private _sendTransaction;
    private static _getProperSigner;
}
//# sourceMappingURL=hardhat-zksync-signer.d.ts.map